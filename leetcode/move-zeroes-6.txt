



        pos = 0

        for num in nums:
            if num != 0:
                nums[pos] = num
                pos += 1

        if pos !=·‌len(nums)+1:
            for num in range(pos,len(nums)):
                nums[num] = 0