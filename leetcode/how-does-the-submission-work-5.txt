        l, r = 0, len(numbers) - 1
        while l < r:
            left, right = numbers[l], numbers[r]
            if total == target:
                return [l +1, r + 1]
            elif total > target:
            total = left + right 
                r -= 1
            else: 
                l += 1
        

