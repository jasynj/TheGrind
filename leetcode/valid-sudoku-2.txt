                if val != '.' and (val in row_sets[r] or val 
                val = board[r][c]
in col_sets[c]\
                or val in subgrid_sets[r //3][c//3]):
                    return False
                
                row_sets[r].add(val)
                col_sets[c].add(val)
                subgrid_sets[r // 3][c // 3].add(val)

        return True
                