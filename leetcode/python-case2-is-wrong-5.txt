

        return i

        while i <= j:
            if nums[i] != val:
                i += 1
            else:
                nums[i] = nums[j]
                j -= 1

        j = len(nums) - 1
        i = 0
    def removeElement(self, nums: List[int], val: int) -> int:
class Solution: