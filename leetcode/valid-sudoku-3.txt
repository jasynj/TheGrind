                if val in rows[r] or val in columns[c] or val in 
                    continue
                if val == ".":
                val = board[r][c]

            for c in range(9):
        for r in range(9):

        subgrids = [[set() for _ in range(3)] for _ in range(3)]
        columns = [set() for _ in range(9)]
        rows = [set() for _ in range(9)]
        
    def isValidSudoku(self, board: List[List[str]]) -> bool:
class Solution: