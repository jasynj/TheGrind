
class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        i = 1

        while i < len(nums):
    
        return len(nums)

            if nums[i] == nums[i-1]:
                nums.pop(nums[i])
            else:
                i +=1